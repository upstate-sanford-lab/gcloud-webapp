sudo apt update
sudo apt install apache2
apache2 -version                                      (to see server version and build)
sudo ufw app list                                     (to see fire wall services)
sudo ufw allow 'Apache'                               (update firewall rules to allow apache)
sudo systemctl status apache2                         (check status of apache)
sudo apt-get install libapache2-mod-wsgi python-dev   (modify apache to work with flask)
cd /var/www/                                          (this is the preferred location for webapp location)
mkdir webApp
cd webApp

mkdir webApp
pwd                                                   (this should show /var/www/webApp/webApp)
---"Move the rest of the webApp structure here"---

---"configure virtual environment"---
pip install virtualenv
virtualenv flaskk
source mypython/bin/activate
pip install -r requirements.txt
deactivate

cd ..
pwd                                                   (this should show /var/www/webApp)
---"Move the webapp.wsgi file here"---
---"Update the path to virual environment"---
---"Update secrete key"---

cd /etc/apache2/sites-available/
---"Move the webApp.conf file here"---
---"Modify admin email"---

sudo service apache2 restart
